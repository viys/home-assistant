
# Loads a standard set of integrations. Keep this entry.
default_config:

# These files store automations, scripts, and scenes created in the UI.
automation: !include automations.yaml
script: !include scripts.yaml
scene: !include scenes.yaml

zha:
  database_path: /config/zigbee.db
  enable_quirks: true
  custom_quirks_path: /config/zha_quirks
  # Optional local OTA providers for Home Assistant 2026.6.x / zigpy.
  # Uncomment one block below only when you need local firmware updates.
  #
  # zigpy_config:
  #   ota:
  #     extra_providers:
  #       - type: advanced
  #         warning: I understand I can *destroy* my devices by enabling OTA updates from files. Some OTA updates can be mistakenly applied to the wrong device, breaking it. I am consciously using this at my own risk.
  #         path: /config/zigpy_ota
  #
  # zigpy_config:
  #   ota:
  #     extra_providers:
  #       - type: z2m_local
  #         index_file: /config/zigpy_ota/index.json
